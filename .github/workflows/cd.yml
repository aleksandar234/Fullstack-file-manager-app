name: CD - Deploy to VM

on:
  workflow_run:
    workflows:
      - CI - Build and Push Docker Images
    types:
      - completed
    branches:
      - cloud-si-separation

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Deploy on VM via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 46.101.134.200
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          port: 22
          script: |
            set -e

            cd /aleksandar/app

            git fetch origin cloud-si-separation
            git reset --hard origin/cloud-si-separation

            kubectl apply -f kubernetes/

            kubectl set image deployment/backend \
              backend=stox234/dzoni-backend:${{ github.event.workflow_run.head_sha }}

            kubectl set image deployment/frontend \
              frontend=stox234/dzoni-frontend:${{ github.event.workflow_run.head_sha }}

            kubectl rollout status deployment/backend
            kubectl rollout status deployment/frontend

            docker image prune -f